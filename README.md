# RandomID Generator

Simple Node.js package to generate random IDs.

## Installation

npm install @malina-gab/randomid-generator

## Usage

const randomID = require('@malina-gab/randomid-generator');

console.log(randomID(10));

Example output:

a8H2Klm9Pq